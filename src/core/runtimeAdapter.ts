// Legacy runtime adapter shim kept for compatibility during incremental
// migration. The canonical `modelToRuntime` now lives in `src/core/modelToRuntime.ts`.
// This file intentionally exports nothing to avoid duplicate exported symbols.
// If you need the adapter, import from `src/core/modelToRuntime` instead.

export {}; 
